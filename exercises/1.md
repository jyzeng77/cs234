# Exercise 1

## Question 1

> Write a pseudocode function Delete All(M, Data) that uses the operations of the ADT Multiset to delete all copies of data item Data from the Multiset M. If there are no copies of the data item Data in M, then M is unchanged.

```txt
DELETE_ALL(M, Data)
Input: a Multiset M and any data item Data
Output: updated M
Effects: all instances of Data are removed from M if they exist

while IS_IN(M, Data):
    DELETE(M, Data)

return M
```

### Corrections and Notes

- It seems I interpreted the question wrong - there shouldn't be an output. Asked about this on Discussions.

## Question 2

> In this question you will augment the ADT Multiset by adding an operation Delete_All(M, Data) that deletes all copies of data Data from the Multiset M. Provide the preconditions and postconditions for Delete_All in two ways:

1. *The operation requires that the data item Data be present*

    | Preconditions | Produces | Effects |
    | -- | -- | -- |
    | the multiset M must exist and the data item Data must be present | modified multiset M | removes all Data from M |

2. *The operation does not require that the data item Data be present*

    | Preconditions | Produces | Effects |
    | -- | -- | -- |
    | the multiset M must exist | either unchanged or modified multiset M | removes all Data if Data exists in M from M |

### Corrections

- For preconditions: write what type each parameter is then write any other conditions they might have, i.e. Data must exist in M for (1)
- The solution uses `deletes` instead of `removes`
- Be specific about what happens usually and any exceptions, i.e. all copies of Data are deleted, but if no copies of Data found, M is unchanged
